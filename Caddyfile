# Replace with your actual domain
collabmd.dev {
	# Sync server WebSocket endpoint — strip /sync prefix
	handle_path /sync/* {
		reverse_proxy sync-server:4444
	}

	# Web app (default)
	reverse_proxy web:3000
}

www.collabmd.dev {
	redir https://collabmd.dev{uri} permanent
}
